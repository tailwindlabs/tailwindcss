import defineClasses from '../util/defineClasses'

export default function() {
  return defineClasses({
    'column-span-all': {
      'column-span': 'all',
    },
    'column-span-none': {
      'column-span': 'none',
    },
  })
}
