import defineClasses from '../util/defineClasses'

export default function() {
  return defineClasses({
    'col-span-all': {
      'column-span': 'all',
    },
    'col-span-none': {
      'column-span': 'none',
    },
  })
}
