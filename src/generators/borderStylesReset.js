import defineClasses from '../util/defineClasses'

export default function() {
  return defineClasses({
    'border-dashed': {
      'border-width': '0'
    },
    'border-dotted': {
      'border-width': '0'
    }
  })
}
