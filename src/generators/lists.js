import defineClasses from '../util/defineClasses'

export default function() {
  return defineClasses({
    'list-reset': {
      'list-style': 'none',
      'margin': '0',
      'padding': '0',
    },
  })
}
