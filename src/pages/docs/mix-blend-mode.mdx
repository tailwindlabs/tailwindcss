---
title: "Mix Blend Mode"
description: "Utilities for controlling how an element should blend with the background."
featureVersion: 'v2.1+'
---

import plugin from 'tailwindcss/lib/plugins/mixBlendMode'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = { plugin }

## Usage

Use the `mix-blend-{mode}` utilities to control how an element's content should be blended with the background.

```html emerald
<template preview>
  <div class="flex justify-center">
    <div class="flex items-center justify-center rounded-md bg-emerald-300 h-24 w-48">
      <div class="mix-blend-multiply rounded-md h-32 w-32 bg-amber-300"></div>
    </div>
  </div>
</template>

<div class="bg-green-300 ...">
  <div class="**mix-blend-multiply** bg-amber-300 ..."></div>
</div>
```

## Responsive

To control the mix-blend-mode property at a specific breakpoint, add a `{screen}:` prefix to any existing mix-blend-mode utility. For example, use `md:mix-blend-overlay` to apply the `mix-blend-overlay` utility at only medium screen sizes and above.

```html
<div class="mix-blend-multiply **md:mix-blend-overlay** ...">
  <!-- ... -->
</div>
```

For more information about Tailwind's responsive design features, check out the [Responsive Design](/docs/responsive-design) documentation.

## Customizing

### Variants

<Variants plugin="mixBlendMode" name="mix-blend-mode" />

### Disabling

<Disabling plugin="mixBlendMode" name="mix-blend-mode" />
