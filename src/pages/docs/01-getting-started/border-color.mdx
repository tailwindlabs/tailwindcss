---
description: Lorem ipsum dolor sit amet
tailwindVersion: v1.2.0+
---

import borderColorPlugin from 'tailwindcss/lib/plugins/borderColor'

export const classes = {
  plugin: borderColorPlugin,
  filterProperties: (prop, value) => prop.indexOf('--') === -1 && value.indexOf('--') === -1,
  preview: (css, { className }) => (
    <td className={`relative w-16 p-2 font-mono text-xs whitespace-pre ${className}`}>
      <div
        class="absolute inset-0 m-2 border"
        style={{
          borderColor: Array.isArray(css['border-color'])
            ? css['border-color'][0]
            : css['border-color'],
        }}
      />
    </td>
  ),
}
