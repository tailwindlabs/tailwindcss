{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["lib/**", "peers/**"]
    },
    "test": {
      "outputs": []
    },
    "oxide:build": {
      "outputs": ["lib/**", "peers/**", "oxide/target/**", "oxide/crates/node/*.node"]
    }
  }
}
