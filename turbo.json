{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "//#build": {
      "outputs": ["lib/**", "peers/**", "types/generated/**"]
    },
    "test": {
      "env": ["OXIDE"],
      "outputs": []
    },
    "//#test": {
      "env": ["OXIDE"],
      "outputs": []
    },
    "//#style": {
      "outputs": []
    }
  }
}
